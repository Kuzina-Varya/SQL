CREATE TABLE IF NOT EXISTS s_psql_dds.t_sql_source_unstructured (
    raw_id         text,
    full_name      text,
    city           text,
    segment        text,
    income         text,
    age            text,
    email          text,
    event_date     text,
    updated_at     text,
    notes          text
);
